{
  "name": "ziawolf-grc-platform",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "types": "wrangler types",
    "db:local": "wrangler d1 execute ziawolf-grc --local --file=schema.sql",
    "db:remote": "wrangler d1 execute ziawolf-grc --remote --file=schema.sql",
    "deploy": "wrangler deploy"
  },
  "devDependencies": { "wrangler": "^4.0.0", "typescript": "^5.0.0", "@cloudflare/workers-types": "^4.20260825.0" }
}
